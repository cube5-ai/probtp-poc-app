# Reading the raw files identify the dimensions types and concepts
# Materialize the dimensions in the database :
# - dimension types (taxonomy, sequence, tags)
# - dimension concepts (including definitions) and their relations to each other (parent, child, related, preceding, following, etc.)

# Output a YAML or JSON file with the dimensions
# This file coule be generated manually